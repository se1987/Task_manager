// @type {import('next').NextConfig}
const nextConfig = {
    reactStrictMode: true, // Reactの厳格モードを有効にする
    experimental: {
      appDir: true, // 新しいApp Router機能を有効にする
    },
  };
  
  export default nextConfig;
